import {describe, it} from "vitest";

// vi.mock();

describe("useZoom", () => {

    it("should set initial values correctly", () => {

    });

    it("should handle scroll event correctly", () => {

    });

    it("should handle drag events correctly", () => {

    });

    it("should stop drag after mouseup event", () => {

    });
});
